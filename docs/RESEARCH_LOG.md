# Flappy Bert — Research / Session Log

Moved out of CLAUDE.md 2026-06-29 for context hygiene. Newest at bottom.

## Roadmap / Changelog (moved from CLAUDE.md)

- **Recently shipped — Aesthetic / creative pass (2026-04-30).** Centralised `FX` module orchestrates particles, audio, screen shake/flash, DOM glow per moment. Magnet powerup (pipe-attached, ~3% from level 2, 5s auto-collect window). Game-over redesign: sequenced reveal with hero score (rainbow shimmer on new best). Pure Web Audio synth upgrades: ADSR / chord / sweep / noiseBurst helpers + synthesised reverb. Spec: `docs/superpowers/specs/2026-04-30-aesthetic-pass-design.md`. Plan: `docs/superpowers/plans/2026-04-30-aesthetic-pass.md`. Backlog of deferred ideas: `docs/superpowers/feature-backlog.md`.

- **Recently shipped — The Summer Session Update (2026-05-29).** Homing HUNTER JEET (tracks altitude, lvl 4+), 2x-score FRENZY powerup (8s, restores prior shop mult), summer aesthetic (warm day sky + sun + brighter day clouds + gold menu season label that flips SEASON 3→4 at Jun 1), and The Summer Session tournament (Jun 1 – Sep 1 2026). Spec: `docs/superpowers/specs/2026-05-29-summer-session-update-design.md`. Plan: `docs/superpowers/plans/2026-05-29-summer-session-update.md`.

- **Recently shipped — PixelLab sprite/aesthetic pass (2026-06-19).** Swapped placeholder art for curated PixelLab.ai pixel sprites, each drawn behind its prior fallback (load/decode failure degrades to the old art — never a broken render): an **animated 5-frame flap Bert** (animates the existing `SPRITE_B64` as the first frame, so all 8 tint-based skins inherit the animation for free; the procedural `drawTail` is skipped on the animated path since the frames bake in a tail), **green-dragon JEET + magenta HUNTER enemy sprites** replacing `fillText('JEETS')` (the homing reticle telegraph is kept), and **coin / magnet / frenzy / shield pickup sprites** replacing the procedural tokens (glow halos kept). World (pipes, parallax city, ground, FX) stays procedural by design. Generated + curated by a multi-agent workflow (per-asset generate-and-judge + adversarial cohesion panel). Spec: `docs/superpowers/specs/2026-06-19-pixellab-aesthetics-design.md`. See the sprite-pipeline note under Important notes.

- **Recently fixed — tournament scores never recorded (2026-06-19).** Game-over fires `submitScoreToServer` + `submitTournamentScore` back-to-back with the SAME single-use `_gameSession` (`flappy_bert.html:2178-2179`); the weekly submit consumed the session, so the tournament submit was rejected (`session_reused`, later `no_session`). Fix: **per-board single-use** (`usedWeekly`/`usedTournament`) so one game records to both boards while replay stays blocked per board. Reproduced + verified by `tools/repro-tournament-bug.cjs`. (Pre-existing since single-use sessions; not caused by the security pass.)

- **Recently shipped — Beat-My-Ghost challenge loop (2026-06-19).** A stateless, Telegram-native "challenge a friend" loop (from the creativity deep-dive). The whole challenge rides in a URL param — **zero new server state/schema**. Share builds `t.me/<bot>?start=g_<sharerId>_<score>`; the bot's `/start` deep-link branch (regex `parseGhost`) resolves the sharer's name from the id (`db.getPlayer`) and replies with a personalized challenge + a `web_app` Play button at `WEBAPP_URL?ghost=…`; the client reads `?ghost=` (or `start_param`), shows a **target pill** (rival + score, gold-pops when passed), a mid-run "PASSED <name>!" celebration, and a "YOU BEAT <name>!" game-over line + the "🎯 CHALLENGE A FRIEND" button (which doubles as challenge-back). The ghost param is **cosmetic/display-only** — it never touches score validation/coins/badges/writes; the shared link is built from the player's OWN verified id + clamped `G.score`. Pure helpers: `lib/ghost-challenge.js` (required by `bot.js` + tested; the client inlines a `parseGhost` mirror). New `GET /api/config` exposes the bot username. Built via subagent-driven TDD (5 tasks, per-task + clean final whole-branch review). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-19-beat-my-ghost*`. Smokes: `tools/ghost-*-smoke.cjs`. **Note:** the share round-trip needs the live bot (`getMe` is null locally).

- **Recently shipped — Tournament score reset + prize ladder (2026-06-19).** The Summer Session leaderboard resets at **00:00 UTC 2026-06-22** (non-destructive time-boundary — scores only count from then once it passes; no cron/wipe/flag) and shows an always-on **1–5 prize ladder** ($100/$60/$40/$30/$20, "— up for grabs —" when empty) in the in-app overlay AND the bot `/tournament` card. Config-driven via two optional `tournaments.json` fields (`scoreResetAt`, `prizes`) — see the Tournaments section. Prizes are **display-only** (no payout system — owner pays winners out-of-band). Backward-compatible (a tournament without the fields is unchanged). Built via subagent-driven TDD (6 tasks, per-task + clean final whole-branch review). Spec/plan: `docs/superpowers/{specs,plans}/2026-06-19-tournament-reset-prizes*`. **Must be deployed before 2026-06-22T00:00:00Z for the boundary to fire on time** (shipped 2026-06-19).

- **Next session — re-triage `docs/superpowers/feature-backlog.md`.** Remaining top candidates: slow-mo powerup (deferred — gameSpeed-lock integration cost), a second enemy (laser JEET, once HUNTER is validated), share-card upgrade, daily login streak, palette-nudge per level.

- **Shipped — Mid-tournament hardening (2026-07-09).** Born from a full 5-dimension critique panel (design/frontend/backend/UX/tests) + hands-on browser play-through. Shipped ONLY the mid-tournament-safe set (constraint: don't make post-change runs harder than the leader's 240 was set under): leaderboard **tiebreak** (earliest-at-best, all 5 queries + RANK() windows, `since`-aware — set BEFORE any 500-cap ties exist), **fixed 60Hz timestep** (was frame-locked: 120Hz = 2x speed, frame-throttling = exploit; 60Hz bit-identical via 1ms epsilon, catch-up cap 4), **crash safety** (`unhandledRejection` keep-alive, `safeSend` on 21 bare Telegram sends, throttled `polling_error`), **`/resettournament <id> CONFIRM` guard**, **auto-pause on backgrounding**, game-over reveal reset (pre-existing: only sequenced on first death/session) + TAP TO SKIP hint, rank-nudge fix (was "N points behind #0" pointing at the LEADER; now nearest attainable rank, 1-based, quiet at score 0), pill restack, copy fixes. Tests 87→119 (TDD, two parallel agents); verified end-to-end via forged-HMAC HTTP drive (tie ranks correctly on both boards, replay/tamper rejected, 2-min 401 storm survived) + Playwright browser drive. **Deferred to Sep 1 season boundary (per CLAUDE.md "Mid-tournament fix policy"): 2x-multiplier-on-tournament removal, difficulty curve past level 20 (plateau at score 200 vs 500 cap), coin economy rebalance (quadratic combo faucet, live+banked double-count, 915-coin one-time sink), 5/sec rate-cap tightening.** Critique also flagged for later: no per-challenge progress counters, out-of-Telegram silent no-save/rotate-wall, single-process bot+API coupling (audit #7), zero bot.js test coverage of the auth/session surface, and two drifted test mirrors (spawn-cap, sequence-runner).

- **Shipped — Faster speed progression, owner-approved mid-tournament exception (2026-07-10).** Trigger: the owner playtested post-timestep-fix and reported the game felt slower ("speed and gravity changed") — root-caused to the 2026-07-09 fixed 60Hz timestep, which took high-refresh players (the majority, on modern phones) off their accidental 1.5–2x game; constants never changed. Decision: rather than revert (would reopen the frame-throttle exploit + refresh-rate unfairness) or raise speed globally to ~2x (would hit 60Hz players with the same disruption in the HARDER direction), the ramp was steepened: **+0.25/level (was +0.15), cap unchanged** — top speed still 5.5, now reached at score 110 not 190; score 50 plays at 4.0 (was 3.4), score 100 at 5.25 (was 4.15); the 190–500 race band is bit-identical (all params already capped there in both curves). This is a recorded EXCEPTION to the mid-tournament fix policy (makes the 50–190 band harder than the leader's-240 conditions) — see CLAUDE.md policy section. Both in-HTML formula sites changed atomically (`updateDifficulty()` + anti-tamper `expectedSpeed()` — the `G.gameSpeed` setter rejects writes above expected+0.5, so a half-applied edit silently freezes the old curve); new mirror + source-sync greps pin them (`tests/lib/difficulty-curve.js`, `tests/difficulty-curve.test.js`; tests 119→127). Server 5/sec score-rate gate confirmed unaffected for the right reason: score cadence is `pipeInterval`-driven (level-based, floor 65 frames @ lvl 15, ~0.92 pipes/s ×2 frenzy ≈ 1.85/s max), not gameSpeed-driven. Ghost challenges (score-target only) and spawn systems (scale off `speed` param) unaffected; JEET/HUNTER approach speeds DO scale, so the effective 50–190 bump is slightly larger than the speed table implies (accepted). Built via subagent-driven TDD (4 tasks, per-task reviews + READY final whole-branch review, post-deploy); browser smoke: `updateDifficulty()` readback 2.75 @ lvl 1 (old: 2.65) with the anti-tamper gate accepting. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-10-speed-progression*`. Doc fix riding along: CLAUDE.md test breakdown said "9 safe-send", actual is 6 (total 127 was already correct).
